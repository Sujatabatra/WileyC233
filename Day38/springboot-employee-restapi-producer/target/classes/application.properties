spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/wileyc233
spring.datasource.username=root
spring.datasource.password=sujata

#ddl-auto:create, if schema exist drop it and recreate the schema and if schema doesnot exist, create the schema
#ddl-auto:update , if schema exist use the existing schema and if schema does not exist create the one.
spring.jpa.hibernate.ddl-auto=update

# converting JPQL to MySql syntax
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.show-sql=true


spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

server.port=8084